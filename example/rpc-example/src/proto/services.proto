syntax = "proto3";

package vshalygin.example.proto;

option cc_generic_services = true;

message message {
    string data = 1;
}

service client_service {
    rpc accept_message (message) returns (message);
}

service server_service {
    rpc accept_message (message) returns (message);
}
